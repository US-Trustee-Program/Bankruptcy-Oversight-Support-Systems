# SQL Edge SA password used by start-services.sh.
# Copy this file: cp .env.template .env
# Then set MSSQL_PASS to a strong password (SQL Server requires complexity).
# Use the same value for ACMS_MSSQL_PASS in ../.env.local.
#
# .env is gitignored — never commit it.

MSSQL_PASS=YourStrong!Passw0rd
