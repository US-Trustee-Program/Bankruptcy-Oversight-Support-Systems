version: '3.8'

# NOTE: This compose file is no longer used by run-e2e-workflow.sh.
# The workflow script uses Podman pods directly for reliable localhost
# networking between SQL Edge, MongoDB, Azurite, and the Functions host.
# This file is kept for reference only.
#
# To run the E2E tests: npm run e2e
services: {}
