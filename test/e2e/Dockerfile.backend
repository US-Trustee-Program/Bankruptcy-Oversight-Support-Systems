# Backend API Dockerfile for E2E Testing
# Cache bust: using host network mode for all services
FROM localhost/e2e_built:latest

WORKDIR /app/backend/function-apps/api

EXPOSE 7071

CMD ["func", "start", "--javascript", "--port", "7071", "--host", "0.0.0.0", "--cors", "http://localhost:3000"]
