name: Build Custom Azure CLI Runner Image

on:
  schedule:
    - cron: '0 5 * * 0'  # Every Sunday at 5:00 AM UTC
  workflow_dispatch:
    inputs:
      azure_cli_version:
        description: 'Azure CLI version to install (use "latest" or leave empty for repository default)'
        required: false
        type: string

env:
  REGISTRY: ghcr.io

permissions: read-all

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    outputs:
      image-tag: ${{ steps.meta.outputs.tags }}
    permissions:
      contents: read
      packages: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0

      - name: Set Azure CLI version
        shell: bash
        env:
          INPUT_VERSION: ${{ github.event.inputs.azure_cli_version }}
          DEFAULT_AZURE_CLI_VERSION: ${{ vars.AZURE_CLI_VERSION }}
        run: |
          if [ -z "${INPUT_VERSION}" ]; then
            # No input provided, use repository variable
            echo "AZURE_CLI_VERSION=${DEFAULT_AZURE_CLI_VERSION}" >> "$GITHUB_ENV"
          else
            echo "AZURE_CLI_VERSION=${INPUT_VERSION}" >> "$GITHUB_ENV"
          fi

      - name: Set lowercase repository name
        shell: bash
        env:
          GITHUB_REPOSITORY_NAME: ${{ github.repository }}
        run: |
          echo "REPO=$(echo "${GITHUB_REPOSITORY_NAME}" | tr '[:upper:]' '[:lower:]')" >> "$GITHUB_ENV"

      - name: Log in to Container Registry
        uses: docker/login-action@ba754150c9dbbaa912ae0ac3cfba43f84195cef2
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@c3b064c6d7c32446d6bc214ea075f1caad2de35a
        with:
          images: ${{ env.REGISTRY }}/${{ env.REPO }}/azure-cli-runner
          tags: |
            type=ref,event=branch
            type=ref,event=pr
            type=sha,prefix={{branch}}-
            type=raw,value=azure-cli-${{ env.AZURE_CLI_VERSION || 'latest' }}
            type=raw,value=latest

      - name: Build and push Docker image
        uses: docker/build-push-action@83bfd93ab480705a7b99a2ed02765137f5ab55ba
        with:
          context: .
          file: .github/docker/Dockerfile.azure-cli
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          build-args: |
            AZURE_CLI_VERSION=${{ env.AZURE_CLI_VERSION }}
