# dev-tools seed config — cosmos-mongo-ustp-cams
# Copy to .env-dev-main and fill in values from the Azure portal / team vault.

# Cosmos — main instance
MONGO_CONNECTION_STRING=

# DXTR SQL Server (shared with dev — same values as .env-dev-local)
MSSQL_HOST=
MSSQL_DATABASE_DXTR=
MSSQL_AUTH_TYPE=azure-active-directory-default
MSSQL_CLIENT_ID=
MSSQL_ENCRYPT=true
MSSQL_TRUST_UNSIGNED_CERT=false

# ACMS SQL Server (shared with dev — same values as .env-dev-local)
ACMS_MSSQL_HOST=
ACMS_MSSQL_DATABASE=
ACMS_MSSQL_AUTH_TYPE=azure-active-directory-default
ACMS_MSSQL_CLIENT_ID=
ACMS_MSSQL_ENCRYPT=true
ACMS_MSSQL_TRUST_UNSIGNED_CERT=false
