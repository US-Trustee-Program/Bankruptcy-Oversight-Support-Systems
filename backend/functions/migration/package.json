{
  "name": "cams-migration",
  "version": "1.0.0",
  "description": "",
  "main": "build/backend/functions/migration/*/*.js",
  "scripts": {
    "clean": "rm -rf build/ dist/ *.zip",
    "prestart": "npm run build && cp ../../.env ../../local.settings.json .",
    "start": "func start --typescript --model v4",
    "build": "tsc --build tsconfig.build.json",
    "pack": "export PACK_OUT=migration; export PACK_TEMP_DIR=/tmp/build/${pwd}; ../../pack.sh"
  }
}
