{
  "name": "cams-migration",
  "version": "1.0.0",
  "description": "",
  "main": "dist/backend/functions/migration/loadConsolidations.js",
  "scripts": {
    "clean": "rm -rf dist/ node_modules/ *.zip",
    "prestart": "npm run build && cp ../../.env ../../local.settings.json .",
    "start": "func start --javascript",
    "build": "tsc --build tsconfig.build.json",
    "pack": "../../pack.sh migration"
  }
}
