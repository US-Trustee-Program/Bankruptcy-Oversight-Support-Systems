{
  "name": "cams-migration",
  "version": "1.0.0",
  "description": "",
  "main": "backend/functions/migration/*/*.js",
  "scripts": {
    "clean": "rm -rf build/ dist/ migration.zip",
    "start": "func start --javascript",
    "build": "tsc --build tsconfig.build.json",
    "pack": "export PACK_OUT=migration; export PACK_TEMP_DIR=/tmp/build/${pwd}; ../../pack.sh"
  }
}
