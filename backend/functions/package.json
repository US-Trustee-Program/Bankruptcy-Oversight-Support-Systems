{
  "name": "node",
  "version": "1.0.0",
  "description": "",
  "devDependencies": {
    "@types/jest": "^29.5.8",
    "@types/mssql": "^9.1.3",
    "@types/node": "^20.9.0",
    "@typescript-eslint/eslint-plugin": "^6.11.0",
    "azure-function-context-mock": "^0.0.7",
    "dependency-cruiser": "^14.1.2",
    "eslint": "^8.53.0",
    "eslint-config-prettier": "^9.0.0",
    "eslint-plugin-import": "^2.29.0",
    "eslint-plugin-n": "^16.3.1",
    "eslint-plugin-prettier": "^5.0.1",
    "eslint-plugin-promise": "^6.1.1",
    "prettier": "^3.1.0",
    "ts-jest": "^29.1.1",
    "typescript": "^5.2.2"
  },
  "dependencies": {
    "@azure/cosmos": "3.17.3",
    "@azure/functions": "^3.5.1",
    "@azure/identity": "^3.4.1",
    "@azure/keyvault-secrets": "^4.7.0",
    "@launchdarkly/node-server-sdk": "^8.2.4",
    "applicationinsights": "^2.9.1",
    "dotenv": "^16.3.1",
    "jest": "^29.7.0",
    "mssql": "^10.0.1"
  },
  "overrides": {
    "@opentelemetry/instrumentation": "0.41.2"
  },
  "scripts": {
    "clean": "rm -rf ./dist & rm -rf ./node_modules",
    "build": "tsc --build tsconfig.build.json",
    "watch": "tsc --project tsconfig.build.json -w",
    "prestart": "npm run build",
    "start": "func start --typescript",
    "start:dev": "nodemon --esm src/server.ts",
    "test": "DATABASE_MOCK='true' jest -c jest.config.js",
    "test:integration": "DATABASE_MOCK='true' jest -c jest.integration.config.js",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "format": "prettier --write './**/*.{js,ts,css,md,json}' --config ./.prettierrc",
    "pack": "mkdir -p /tmp/build && zip -q -r /tmp/build/$OUT.zip . --exclude @.funcignore --exclude .funcignore && mv /tmp/build/$OUT.zip .",
    "coverage": "DATABASE_MOCK='true' jest --coverage",
    "dependency-cruiser-graphical": "current_time=$(date +'%Y-%m-%d_%H%M') && npx depcruise --output-type dot --do-not-follow '^node_modules($|/)' attorneys case-assignments cases healthcheck lib | dot -T svg > ../../docs/architecture/dependency-cruiser/functions/dependency-graph_api_${current_time}.svg",
    "dependency-cruiser-ci": "npx depcruise --output-type err-long attorneys case-assignments cases healthcheck lib "
  }
}
