{
  "name": "cams-api",
  "version": "1.0.0",
  "description": "",
  "main": "backend/functions/api/*/*.js",
  "scripts": {
    "clean": "rm -rf build/ dist/ api.zip",
    "start": "func start --javascript",
    "build": "tsc --build tsconfig.build.json",
    "pack": "export PACK_OUT=api; export PACK_TEMP_DIR=/tmp/build/${pwd}; ../../pack.sh"
  }
}
