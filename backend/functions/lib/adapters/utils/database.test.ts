const context = require('azure-function-context-mock');
import {list} from './../../testing/mock-data/cases.mock';
import { executeQuery } from "./database";
import { DbTableFieldSpec, QueryResults } from '../types/database';

describe('Test Database module executeQuery function', () => {
  xtest('executeQuery function should return results as "QueryResults" for an input query', () => {


  });
})
